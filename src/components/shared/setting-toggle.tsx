'use client';

/**
 * Die An/Aus-Zeile stammt aus dem Designsystem. Hier steht nur die
 * Weiterleitung, damit die Aufrufstellen im Dashboard weiter über
 * '@/components/shared/...' importieren und ein späterer Wechsel der
 * Quelle an einer Stelle passiert.
 */
export { SettingToggle, type SettingToggleProps } from '@openeos/ui';
